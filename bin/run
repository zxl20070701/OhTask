#!/usr/bin/env node

'use strict';

process.title = 'OhTask';

// 启动配置
if (process.argv[2] === "config") {
    require("./config.js")();
}

// 服务器模式
else if (process.argv[2] === "serve") {
    require("./serve.js")();
}

// 命令行模式
else {
    require("./cli.js")();
}